# Dockerfile.dev - untuk development dengan Air (hot-reload)
FROM golang:1.22-alpine

RUN apk add --no-cache git curl

# Install Air untuk hot-reload
RUN go install github.com/air-verse/air@latest

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

EXPOSE 8080

CMD ["air", "-c", ".air.toml"]
